import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'SELEkT',
  webDir: 'dist/selek-t-app', // Asegúrate de que este valor sea correcto
};

export default config;
